name: learn-github-actions
on: [push]

jobs:
  update-projects:
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ secrets.USERNAME }}
    steps:
      - name: Print the input variable
        uses: AxiomSamarth/go-vendor-action@main
        with:
          variable: Samarth Deyagond
                    
        


          
